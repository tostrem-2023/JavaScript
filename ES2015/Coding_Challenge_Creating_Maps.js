const Person = new Map ([
    ["firstName", "Nolan"], // key is text so it must be in quotes
    ["lastName", "Ostrem"],
    ["city", "Maple Valley"],
    ["mobileNumber", "253.653.3101"]
]);

console.log(Person.keys());
console.log(Person.values());