// IIFE = Immediately Invoke Function Expression

(function () {  // anonymous function; doesn't require a name because you want to execute automatically, and will not use later on
    var a = "Hello"  // variable is not in global scope
    console.log(a);
}) ();